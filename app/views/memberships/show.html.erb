<p>
  <strong>User:</strong>
  <%= @membership.user.email %>
</p>

<p>
  <strong>Organisation:</strong>
  <%= @membership.organisation.name %>
</p>

<p>
  <strong>Group:</strong>
  <%= @membership.group.name if @membership.group.present? %>
</p>

<%= link_to 'Edit', edit_membership_path(@membership) %> |
<%= link_to 'Back', memberships_path %>
